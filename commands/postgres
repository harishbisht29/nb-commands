-- Grant permissions on all existing tables
GRANT SELECT, UPDATE ON ALL TABLES IN SCHEMA public TO your_role;

-- Grant permissions on future tables
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, UPDATE ON TABLES TO your_role;

